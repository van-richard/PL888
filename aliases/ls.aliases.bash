#/bin/bash

alias l="ls -CF"
alias la="ls -la"
alias ll="ls -alF"
alias lt="ls -lt" 

alias lta="ls -lta"
alias lat="lta"

alias ltd="ls -ltd"
alias ldt="ltd"

alias lth="ls -lt | head"
alias lht="lth"

alias ltt="ls -lt | tail"

