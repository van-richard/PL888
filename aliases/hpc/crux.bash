#!/usr/bin/env bash

# Crux uses PBS and primarily Cray compiler environments. Shared PBS helpers
# are loaded from aliases/scheduler/pbs.bash when qsub is available. Add
# Crux-only aliases here if they become necessary.
