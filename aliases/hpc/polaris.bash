#!/usr/bin/env bash

# Polaris uses PBS and primarily Cray compiler environments. Shared PBS
# helpers are loaded from aliases/scheduler/pbs.bash when qsub is available.
# Add Polaris-only aliases here if they become necessary.
